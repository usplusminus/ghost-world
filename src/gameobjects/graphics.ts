import {HexColor} from "../scenes/colors";

export const debugGraphicsConfig = {
    fillStyle: {color: HexColor.red},
    lineStyle: {width: 0.5, color: HexColor.red, alpha: 1.0}
}

export const whiteStrokeGraphicsConfig = {
    lineStyle: {width: 1.0, color: HexColor.white, alpha: 1.0}
}
